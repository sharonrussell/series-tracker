# Tasks

## 1. Dropped progress presentation

- [x] 1.1 Keep the numeric progress fraction visible for dropped rows and render `Dropped` beneath it using the existing secondary-note treatment; verify reading, completed, up-to-date, and dropped display branches with focused tests.

## 2. Validation

- [x] 2.1 Refresh the running UI and smoke test the dashboard; verify dropped rows show both progress and `Dropped`, other rows retain their existing indicators, row navigation remains accessible, and the browser console is clean.
