# Tasks

## 1. Dashboard list refinement

- [x] 1.1 Update the dashboard list markup to remove the status column and verify the remaining headers match the simplified list layout.
- [x] 1.2 Adjust the row rendering so a series at 100% progress displays the completion state as completed and the row uses the completed green visual treatment.
- [x] 1.3 Refresh the running UI, smoke test the dashboard, and verify the list still shows title, author, publication state, and progress without the redundant status column.
- [x] 1.4 Show a secondary `to read` or `Up to date` indicator in the progress cell without adding detail to the column title, then refresh the UI and smoke test it.
